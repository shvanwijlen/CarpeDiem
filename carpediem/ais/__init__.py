"""AIS nearby-vessel tracking, ported from ais_nearby_vessels_7.ino and
merged with the em-trak GPS handling that used to live in the main
CarpeDiem sketch (connectToEmtrak(), the DoGPS block in loop()).

See service.py for the entry point (`AisService`), used by main.py.
"""
